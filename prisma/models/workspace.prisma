model Workspace {
    id Int @default(autoincrement())
    org_Id String @id @default(uuid()) @unique
    Name String
    user_Id String
    Role WorkspaceRole
    user User @relation(fields:[user_Id] , references:[user_id] onDelete)
}

enum WorkspaceRole {
    PERSONAL
    ORGANIZATION
}